name: Secret scan

on:
  push:
    branches: ["1.1"]
  pull_request:

jobs:
  trufflehog:
    uses: DoubtfulTurnip/.github/.github/workflows/trufflehog-scan.yml@main
    permissions:
      contents: read
